# backup
This project is an ** AI CLI skill ** that provides automated backup capabilities for arbitrary directories.It creates a time-stamped ZIP archive of the current working directory, automatically rotates backups to retain the last 10 copies, and excludes the backup directory itself when archiving.
